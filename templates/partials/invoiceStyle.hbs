<style>
  /* GENERAL */ .center { text-align: center !important; } .right { text-align:
  right !important; } /* INVOICE */ :root { font-family: "Segoe UI", Tahoma,
  Geneva, Verdana, sans-serif; --main: #15803d; --gray-text: rgb(102, 102, 102);
  --light: #28af59; } /* INVOICE HEADER */ #header h1 { font-size: 3rem;
  font-weight: bold; margin-bottom: 0.5rem; } #header h2 { font-size: 1.5rem;
  font-weight: 400; } #header-top { display: flex; justify-content:
  space-between; align-items: center; } #header-top-title { background-color:
  var(--main); color: white; padding: 1rem; width: 40%; } #header-top-number {
  padding: 1rem; font-size: 2rem; font-weight: bold; } #header-info { display:
  flex; justify-content: space-between; padding: 0 1rem; margin-top: 1rem; }
  #header-info > div:last-child { text-align: right; } .label { font-weight:
  600; font-size: 0.9rem; } .value-display > .label { margin-bottom: 0.2rem; }
  .value-display { margin-bottom: 0.5rem; } /* dr */ .dr-section { margin-top:
  1rem; } .dr-section__header { background-color: var(--light); color: white;
  font-weight: bold; padding: 0.5rem 1rem; width: 40%; }
  .dr-section__header--right { margin-left: auto; border-top-left-radius: 1rem;
  border-bottom-left-radius: 1rem; text-align: right; }
  .dr-section__header--left { margin-right: auto; border-top-right-radius: 1rem;
  border-bottom-right-radius: 1rem; text-align: left; } .dr-section .delivery {
  padding: 0 1rem; margin-top: 1rem; } .dr-section .delivery header { display:
  flex; justify-content: space-between; } .dr-section .delivery header > div >
  span { display: block; } .dr-section .delivery header > div > span:first-child
  { font-size: 0.8rem; color: var(--gray-text); } .dr-section .delivery header >
  div > span:last-child { font-size: 0.9rem; font-weight: 500; } .delivery table
  { width: 100%; margin-top: 0.5rem; } .delivery table th, .delivery table td {
  text-align: right; } .delivery table thead th { background-color: var(--main);
  color: white; padding: 0.5rem 0.2rem; font-weight: 600; text-transform:
  uppercase; font-size: 0.8rem; } .delivery table tbody tr:nth-child(even) {
  background-color: whitesmoke; } .delivery table tbody td, .delivery table
  tfoot th, .delivery table tfoot td { padding: 0.2rem; } .delivery table thead
  th:first-child, .delivery table tbody td:first-child { text-align: left; }
  .delivery table tfoot th { text-align: right; font-weight: 600; } .delivery
  table tfoot th, .delivery table tfoot td { font-weight: 600; } .delivery table
  td:last-child, .delivery table th:last-child { text-align: right; }
  .table__result { font-size: 1.2rem; } #grand-total { width: 60%;
  background-color: var(--main); color: white; font-weight: bold; padding: 1rem;
  margin-left: auto; margin-top: 3rem; text-align: right;
  border-bottom-left-radius: 2rem; } #grand-total > *:first-child { font-size:
  1.5rem; } #grand-total > *:last-child { font-size: 2rem; }

</style>